# v0.6.2 - Fix White Screen Issue (SSR Disabled)

## Date
2026-03-10

## Description
Fixed the white screen issue that prevented the application from loading any content.

## Issue Details

**Symptoms:**
- Application displayed only a white screen with no error messages
- No content visible on any page
- Browser console showed no obvious errors

**Root Cause:**
The Inertia SSR (Server Side Rendering) was enabled in `config/inertia.php` and configured to connect to `http://127.0.0.1:13714`, but the SSR server was not running. This caused all page requests to fail silently, resulting in a white screen.

## Changes Made

### config/inertia.php
- Changed `'enabled' => true` to `'enabled' => false` in the SSR configuration
- Disabled SSR to prevent white screen when SSR server is not running
- Added inline comment explaining the change

### Cache Cleared
- Executed `php artisan config:clear` to clear configuration cache
- Executed `php artisan cache:clear` to clear application cache

## Technical Details

**Why This Fixes the Issue:**
1. Inertia was attempting to render pages server-side via SSR
2. When the SSR server at `http://127.0.0.1:13714` was unavailable, requests failed
3. In production mode with `APP_DEBUG=false`, errors were suppressed
4. This resulted in a completely blank page with no error messages

**Client-Side Rendering (CSR):**
- With SSR disabled, Inertia falls back to client-side rendering
- This is the default and most common mode for Inertia.js applications
- The application now loads normally without requiring a separate SSR server

**Future SSR Implementation:**
If SSR is desired in the future for SEO or performance:
1. Start the SSR server: `npm run build:ssr` then `node bootstrap/ssr/ssr.mjs`
2. Update `config/inertia.php` to set `'enabled' => true`
3. Ensure the SSR server URL matches the running server
4. Or use Laravel's built-in SSR bundle configuration instead of URL

## Testing
- Application now loads correctly with white screen resolved
- All pages accessible (welcome, login, register, dashboard, etc.)
- Navigation works properly
- No white screen issues after the fix

## Notes
- SSR is optional and not required for most Inertia.js applications
- Client-side rendering provides a faster development experience
- SSR can be re-enabled later if needed for production performance optimization

## Related Files
- `config/inertia.php` - Modified SSR configuration
- `vite.config.ts` - Contains SSR build configuration
- `resources/js/ssr.tsx` - SSR entry point (not used when disabled)